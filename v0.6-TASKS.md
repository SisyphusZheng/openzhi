# v0.6 Architecture Review — Task List

**来源**: ARCHITECTURE-REVIEW.md (2026-05-06 全面架构审查)
**原则**: Web Standards first — 优先采用 WHATWG HTML Living Standard 原生能力
**状态**: ✅ ALL COMPLETE
**版本**: v0.6.0-alpha.1

---

## Phase 1: Signals 系统重构 (P0) ✅ DONE

| # | 任务 | 状态 | 备注 |
|---|------|------|------|
| 1.1 | signal-polyfill 二开 — 内联 TC39 引擎到 @lessjs/signals | ✅ | Signal.State + Signal.Computed + Signal.subtle.Watcher |
| 1.2 | `signal()` / `computed()` — 自动依赖追踪 | ✅ | .value 语法，subscribe() 便捷方法 |
| 1.3 | `effect()` — 真正的响应式 effect（依赖变化自动重跑） | ✅ | 基于 Watcher + queueMicrotask |
| 1.4 | `islandEffect()` — Island 生命周期绑定 | ✅ | disconnect 时自动清理 |
| 1.5 | `channel()` 保留 — CustomEvent 广播拆为独立实现 | ✅ | 与 Signal 正交 |
| 1.6 | `themeSignal` 重写 | ✅ | 用新 signal 引擎 |
| 1.7 | 浏览器原生 Signal 条件回退 | ✅ | 引擎层检测 globalThis.Signal |
| 1.8 | 移除旧 API — derived() + fire-once effect() | ✅ | 完全删除 |

---

## Phase 2: DSD 规范对齐 (P0) ✅ DONE

| # | 任务 | 状态 | 备注 |
|---|------|------|------|
| 2.1 | `shadowrootdelegatesfocus` — 交互组件 | ✅ | less-button, less-input, less-theme-toggle |
| 2.2 | `shadowrootserializable` | ✅ | DsdOptions 接口支持 |
| 2.3 | `shadowrootslotassignment="manual"` | ✅ | DsdOptions 接口支持 |
| 2.4 | `shadowrootcustomelementregistry` — scoped registry | ✅ | DsdOptions 接口支持 |
| 2.5 | `inferDsdOptions()` — 从组件类静态属性推断 | ✅ | delegatesFocus, serializable, slotAssignment, customElementRegistry |

---

## Phase 3: Form-Associated CE + :state() (P1) ✅ DONE

| # | 任务 | 状态 | 备注 |
|---|------|------|------|
| 3.1 | less-button formAssociated + ElementInternals | ✅ | type="submit" 在 <form> 中可用 |
| 3.2 | less-input :state() 支持 | ✅ | disabled/enabled/invalid 状态 |
| 3.3 | less-button :state() 支持 | ✅ | disabled/enabled 状态 |
| 3.4 | CSS :state() 伪类样式 | ✅ | :host(:state(disabled)), :host(:state(invalid)) |

---

## Phase 4: Speculative Loading + Navigation API (P1) ✅ DONE

| # | 任务 | 状态 | 备注 |
|---|------|------|------|
| 4.1 | `<link rel="modulepreload">` — eager islands | ✅ | injectSpeculativeLinks() |
| 4.2 | `<link rel="prefetch">` — lazy/visible/idle islands | ✅ | injectSpeculativeLinks() |
| 4.3 | Navigation API 集成 | ✅ | navigate(), onNavigate(), matchRoute() |
| 4.4 | URLPattern 路由匹配 | ✅ | matchRoute() 支持 URLPattern + regex fallback |

---

## Phase 5: parse5 + URLPattern (P1) ✅ DONE

| # | 任务 | 状态 | 备注 |
|---|------|------|------|
| 5.1 | parse5 re-export from ssg-postprocess | ✅ | export { parse, serialize } from 'parse5' |
| 5.2 | URLPattern 路由匹配 | ✅ | 在 navigation.ts 中实现 |
| 5.3 | 路由扫描器 URLPattern 注释 | ✅ | filePathToRoutePath 兼容 URLPattern |

---

## Phase 6: dialog/popover + inert (P1) ✅ DONE

| # | 任务 | 状态 | 备注 |
|---|------|------|------|
| 6.1 | less-dialog 组件 | ✅ | 原生 <dialog> + ::backdrop + popover |
| 6.2 | dialog :state() 支持 | ✅ | open/closed 状态 |
| 6.3 | dialog inert 属性 | ✅ | 打开时 siblings 标记 inert |
| 6.4 | less-layout mobile menu inert | ✅ | 菜单打开时 .layout-main 添加 inert |

---

## Phase 7: Island 系统改进 (P1) ✅ DONE

| # | 任务 | 状态 | 备注 |
|---|------|------|------|
| 7.1 | Mixin 替代猴子补丁 | ✅ | __lessIslandWrapped 防重入 |
| 7.2 | visible 策略观察所有实例 | ✅ | querySelectorAll + forEach observe |
| 7.3 | 适配器显式注入 | ✅ | registerAdapter() + RenderAdapter 接口 |
| 7.4 | idle 降级改进 | ✅ | rIC → rAF → setTimeout(50) |
| 7.5 | globalThis 适配器向后兼容 | ✅ | getAdapter() 优先 explicit，回退 globalThis |

---

## Phase 8: Quick Wins (P0/P1) ✅ DONE

| # | 任务 | 状态 | 备注 |
|---|------|------|------|
| 8.1 | prefers-color-scheme 检测 | ✅ | matchMedia('(prefers-color-scheme: light)') |
| 8.2 | color-scheme 属性设置 | ✅ | document.documentElement.style.colorScheme |
| 8.3 | 清理 @lit-labs/ssr-dom-shim | ✅ | 从 deno.json 移除 |
| 8.4 | 统一 escapeHtml/escapeAttr | ✅ | adapter-lit 从 render-dsd import |
| 8.5 | 语义化 HTML — less-layout | ✅ | div→nav, div→footer |
| 8.6 | CSS 去重（injectLayoutStyles） | ✅ | color-values.ts 零依赖拆出，SSG 动态生成，消除了硬编码 |

---

## 测试结果

```
322 passed (95 steps) | 0 failed
deno fmt ✓
deno test ✓
```

---

*基于 ARCHITECTURE-REVIEW.md (2026-05-06) 执行完成。所有 WHATWG HTML Living Standard 对齐任务已完成。*
