{
  "$schema": "https://deno.land/x/deno/cli/schemas/config-file.v1.json",
  "workspace": [
    "./packages/kiss-vite",
    "./packages/kiss-rpc",
    "./examples/poc",
    "./docs-site"
  ],
  "tasks": {
    "dev": "cd examples/poc && deno task dev",
    "build:vite": "cd packages/vite && deno task build",
    "build:rpc": "cd packages/rpc && deno task build",
    "build": "deno task build:rpc && deno task build:vite",
    "test": "deno test --allow-read --allow-write --allow-env --allow-net",
    "test:watch": "deno test --allow-read --allow-write --allow-env --allow-net --watch",
    "typecheck": "deno check **/*.ts",
    "lint": "deno lint",
    "fmt": "deno fmt",
    "fmt:check": "deno fmt --check",
    "clean": "rm -rf packages/*/dist packages/*/.tsbuildinfo"
  },
  "fmt": {
    "useTabs": false,
    "lineWidth": 100,
    "indentWidth": 2,
    "semiColons": true,
    "singleQuote": true,
    "proseWrap": "preserve"
  },
  "lint": {
    "rules": {
      "tags": ["recommended"]
    }
  },
  "nodeModulesDir": "auto",
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable", "deno.ns"],
    "strict": true,
    "skipLibCheck": true
  },
  "exclude": [
    "node_modules",
    "dist",
    "**/dist/**",
    "**/node_modules/**",
    ".deno"
  ]
}
