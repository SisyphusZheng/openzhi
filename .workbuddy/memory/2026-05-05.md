# 2026-05-05 KISS Working Log

## v0.5 Alpha Trust Release 完成

### Bug 修复 (从上一会话延续)
1. 根 middleware scope `'//*'` → `'/*'` (entry-renderer.ts)
2. buildIslandChunkMap 双前缀 → entry.file 已含 islands/ 前缀 (ssg-postprocess.ts)
3. Island strategy 传递 → package metadata → ClientIslandEntry (build-client.ts)
4. 嵌套 island 路径 → islandFiles 参数 (entry-descriptor.ts, index.ts, hono-entry.ts)
5. SSG 丢失 CSP 注入 → injectCspMeta() (build-ssg.ts)
6. DEFAULT_NAV 同步 → 7-section (kiss-layout.ts)
7. escapeHtml 注释统一 (adapter-lit/ssr.ts)

### 本次修复 (continuation session)
8. DEFAULT_NAV 测试断言更新 → section 'Strategy' 替代 'Decisions' (components.test.ts)
9. lit html 模板反引号修复 → `` ` `` → `└` (getting-started.ts, error-handling.ts)

### 构建验证
- 309/310 tests passed (1 Deno upstream signal leak)
- `deno task build` 全流程通过: Phase 1 (vite build) → Phase 2 (build-client) → Phase 3 (build-ssg)
- 40 pages, 9 islands, 67 KB JS total
- Commit: 0065b50

### Trust Release 状态
文档承诺 ↔ 构建产物对齐完成。核心 Trust Gap 已消除。

---

## Remote 迁移：SisyphusZheng/kiss → lessjs-run/LessJS

仓库从 `git@github.com:SisyphusZheng/kiss.git` 迁移到 `git@github.com:lessjs-run/LessJS.git`。
操作方式：直接修改 remote URL 后 fetch + reset。
变更内容：
- 新增包：`packages/kiss-adapter-lit` (v0.2.0)
- 移除：空目录 `packages/vite/`
- 新增：`demo/` 目录
- 版本更新：`@kissjs/core` v0.3.1→0.5.3, `@kissjs/ui` v0.3.1→0.5.2
- 新增 tag：v0.3.2 ~ v0.5.3-trustd 共 11 个 tag
- 最新 commit：f4915eb6 chore: remove temporary MD files from repo

---

## LessJS 品牌迁移 + JSR/npm 双发配置 (2026-05-05)

### 已完成的工作

**Phase 1: 代码库重命名**
- `@kissjs/*` → `@lessjs/*` 全局替换（src、test、docs、CI）
- 目录重命名：`kiss-core`→`core`, `kiss-rpc`→`rpc`, `kiss-ui`→`ui`, `kiss-adapter-lit`→`adapter-lit`, `create-kiss`→`create`
- deno.json workspace/tasks 路径更新
- CI workflow 文件路径更新（test.yml, publish.yml, lint.yml, deploy.yml）
- 旧仓库链接修复：`SisyphusZheng/kiss` → `lessjs-run/LessJS`（README badge, contributing, kiss-layout githubUrl 等）
- CI badge URL 已修复指向新仓库

**Phase 2: dnt npm 发布脚本**
- `packages/rpc/_build_npm.ts` — 零依赖，最简单
- `packages/core/_build_npm.ts` — 需 Node shim + npm deps (vite, hono, parse5, entities)
- `packages/ui/_build_npm.ts` — 预构建 dist/ → 直接打包
- `packages/adapter-lit/_build_npm.ts` — 零依赖 duck-type
- `packages/create/_build_npm.ts` — CLI 包装器，需 deno 作为前置

**Phase 3: CI 双发自动化**
- `publish.yml` 已改为 JSR → npm 顺序双发
- npm publish 需要 GitHub repo 配置 `NPM_TOKEN` secret
- 发布顺序：rpc → ui → adapter-lit → core → create（按依赖顺序）

### 待办
- [ ] 在 GitHub repo settings 添加 `NPM_TOKEN` secret
- [ ] 第一个双发测试（建议从 rpc 开始）

---

## 文档中文化 + 构建修复 (2026-05-05 下午)

### 文档中文化
- README.md 改为中文优先（原 README.zh-CN.md 的内容）
- 英文版移至 README.en.md
- Guide 页面标题全部中文化（架构、路由、部署、测试等 17 页）
- `packages/create/README.md` 重写为中文 + LessJS 品牌

### 构建修复
- SSG Phase 3 的 `customElements.define` 重复注册问题：在 build-ssg 管线层面 patch
- `packages/ui/deno.json` 补充 vite-plugin-dts 依赖声明
- 清理了残留的 `@kissjs` node_modules 缓存

### 构建验证结果
- ✅ `deno task build` 全量构建通过（40 pages）
- ✅ `<title>LessJS</title>` 确认
- ✅ **310/310 tests passed（0 failed）**
- ✅ `packages/rpc/_build_npm.ts` dnt 成功生成 npm 包结构
- ✅ JSR @lessjs scope 已创建（5 包名已保留，待首次 publish）
